<%= form_for(@user, :url => {:action => 'login'}) do |f| %>
 
  <div class="field">
    <%= f.label :username %><br />
    <%= f.text_field :username %>
  </div>
  <div class="actions">
    <%= f.submit("Login") %>
  </div>
<% end %>
