<%= form_for([@client,@content_asset]) do |f| %>
  
  

  <% if @content_asset.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@content_asset.errors.count, "error") %> prohibited this content_asset from being saved:</h2>

      <ul>
      <% @content_asset.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>


   <div class="field">
    <%= f.label :handle %><br />
    <%= f.text_field :handle %>
  </div>

 <div class="field">
    <%= f.label :client_id %><br />
    <%= f.text_field :client_id %>
  </div>

 <div class="field">
    <%= f.label :body %><br />
    <%= f.text_area :body %>
  </div>


  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
