<!DOCTYPE html>
<html>
<head>
  <title>Mindspring</title>
  <%= stylesheet_link_tag :all %>
  <%= javascript_include_tag :defaults %>
  <%= csrf_meta_tag %>
  <% unless session[:user_id].nil? %>
  <% @user = User.find(session[:user_id]) %>
    <link rel="stylesheet" type="text/css" href="<%=style_client_path(@user.client,:format => :css)-%>" />
  <% end %>
 </head>
<body>

<%= yield %>

</body>
</html>
