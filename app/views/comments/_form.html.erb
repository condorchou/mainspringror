<%
#used for writing comments
  @user = User.find(session[:user_id]) if @user.nil? 
  @comment = @video.comments.build(:user_id => @user.id) if @comment.nil?
%>
<%= form_for([@client, @video, @comment]) do |f| %>
  <% if @comment.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@comment.errors.count, "error") %> prohibited this clients_video from being saved:</h2>

      <ul>
      <% @comment.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :body %><br />
    <%= f.text_area :body %>
  </div>
    <%= f.hidden_field :video_id %>
    <%= f.hidden_field :user_id %>


  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
